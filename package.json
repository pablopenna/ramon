{
  "name": "cpu-emulator",
  "version": "1.0.0",
  "private": true,
  "description": "In-browser AArch64 assemble (Keystone) + emulate (Unicorn) round-trip, proven headlessly under Node.",
  "scripts": {
    "test": "node verify.js && node verify-keystone-wasm.js && node verify-wasm.js",
    "serve": "python3 -m http.server 8000"
  }
}
