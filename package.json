{
  "name": "cpu-emulator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "In-browser AArch64 assembly learning environment: assemble (Keystone WASM) + emulate (Unicorn asm.js), client-side only.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "proof": "tsx scripts/headless-proof.ts",
    "proof:arm32": "tsx scripts/headless-proof-arm32.ts",
    "spike:test": "node verify.js && node verify-keystone-wasm.js && node verify-wasm.js"
  },
  "dependencies": {
    "@codemirror/state": "^6.6.0",
    "@codemirror/view": "^6.43.0",
    "codemirror": "^6.0.2"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vite": "^6.0.5"
  }
}
